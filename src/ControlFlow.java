
public class ControlFlow {
        //Comparison operators
        /*
         *  (equals to) ==
         * (not equals to) !=
         * (greater than) > or >=
         * (less than) < or <=
         * */

        // Logical operators
        /*
         * (AND) && {Both conditions need to be true to return true}
         * (OR) || {if one condition is true it returns true}
         * (NOT) ! {Applied to a comdition to make it false}
         * */

        // FizzBuzz
//
//        Scanner scanner = new Scanner(System.in);
//        System.out.print("Number: ");
//        int number = scanner.nextInt();
//
//        if (number % 3 == 0 && number % 5 == 0)
//            System.out.println("FizzBuzz");
//        else if (number % 5 == 0 )
//            System.out.println("Fizz");
//        else if (number % 3 == 0)
//            System.out.println("Buzz");
//        else
//            System.out.println(number);

        // For loops - to repeat one or more statements / for iterating
//        for (int i = 0; i < 5; i++)
//            System.out.println("Hello world");

        // While loops - if we do not know how many times a user is going to enter something.
//        String input = "";
//        while (!input.equals("quit")){
//            System.out.print("Input: ");
//            input = scanner.next().toLowerCase();
//            System.out.println(input);
//        }

        // Do while loops - these get executed at least once
//        do {
//            System.out.print("Input: ");
//            System.out.println(input);
//        }while (!input.equals("quit"));

        // Break & Continue
//        while (true) {
//            System.out.print("Input: ");
//            input = scanner.next();
//            if (input.equals("pass"))
//                continue;
//            if (input.equals("quit"))
//                break;
//            System.out.println(input);

            // For each loops
// Limitations: it only moves forward; whe cannot access the index of items
//        String[] fruits = {"Apple","Banana","Orange"};
//        for (String fruit : fruits) {
//            System.out.println(fruit);
//        }


//        }
    }
